import React from 'react';
import ReactDOM from 'react-dom';

import styles from '../styles';

const Footer = () => (
  <div style={styles.footer}>
    <div>contact | about | resume</div>
  </div>
);

export default Footer;